dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install python-mock
    - virtualenv .venv
    - source .venv/bin/activate
checkout:
  post:
    - git submodule sync
    - git submodule update --init
