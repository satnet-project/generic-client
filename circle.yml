general:
  branches:
    only:
      - master
      - development

dependencies:
  pre:
    - sudo apt-get install python-mock
    - sudo apt-get install python-twisted
    - cd scripts && bash ./setup.sh -circleCI

test:
  override:
    - cd tests/ && trial test_readDataFromFile
    - cd tests/ && trial test_readDataFromTerminal
    - cd tests/ && trial test_notifyMsgUsage
    - cd tests/ && trial test_saveReceivedFrames
    - cd tests/ && trial test_connectionDown
    - cd tests/ && trial test_connectionEstablished
    - cd tests/ && trial test_manageFrames
    - cd tests/ && trial test_userinterfacesparameters
    - cd tests/ && trial test_userinterfacesoperation
    - cd tests/ && trial test_userinterfacesparameters
    - cd tests/ && trial test_userinterfacesconnection
    - cd tests/ && trial test_userinterfacedisconnection
    - cd tests/ && trial test_userinterfaceclosewindow
    - cd tests/ && trial test_userinterfacesthreads
    - cd tests/ && trial test_threadsoperation
    - cd tests/ && trial test_configurationWindowOperation