language: 'python'

branches:
  only:
    - master

python:
  - '2.7'

git:
  submodules: false

install:
  - pip install coveralls
  - cd scripts/
  - bash ./setup.sh -t
  - cd ../tests
  - source /home/travis/virtualenv/bin/activate

script:
  - coverage run test_clientToServer.py

after_success:
  - cd ../../
  - 'coveralls'
