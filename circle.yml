dependencies:
  pre:
    - sudo apt install mock
    - sudo apt install kiss
    - virtualenv .venv
    - source .venv/bin/activate
checkout:
  post:
    - git submodule sync
    - git submodule update --init
