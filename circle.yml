dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install mock
    - sudo apt-get install kiss
    - virtualenv .venv
    - source .venv/bin/activate
checkout:
  post:
    - git submodule sync
    - git submodule update --init
