language: 'python'
branches:
  only:
    - master
python:
  - '2.7'


#  - './scripts/setup.sh'
#before_script:
#  - "psql -c 'create database satnet_db;' -U postgres"
#  - 'python manage.py migrate'

# Handle git submodules yourself
git:
    submodules: false
# Use sed to replace the SSH URL with the public URL, then initialize submodules
before_install:
    - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
    - git submodule update --init --recursive
    - cd protocol
    - git checkout
    - cd ../
install:
  - 'pip install -r requirements.txt'

script:
  - 'python protocol/tests/test_creds.py test'
  - 'python protocol/tests/test_end2end.py test'
  - 'python protocol/tests/test_login_multipleClients.py test'
  - 'python protocol/tests/test_login_singleClient.py test'
  - 'python protocol/tests/test_sendMsg.py test'
  - 'python protocol/tests/test_startRemote.py test'
#  - 'coverage erase'
#  - 'coverage run manage.py test'
#after_script:
#  - 'coveralls'
